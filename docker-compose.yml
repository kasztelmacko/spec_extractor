services:
  spec-extractor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spec-extractor
    volumes:
      - .:/app
      - uv-cache:/root/.cache/uv
    working_dir: /app
    stdin_open: true
    tty: true
    command: /bin/bash

volumes:
  uv-cache:

